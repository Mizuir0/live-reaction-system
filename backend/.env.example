# ローカル開発用の環境変数サンプル
# 本番環境ではRenderの環境変数設定で管理します

# データベースURL (PostgreSQLを使う場合)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# データベースURL (SQLiteを使う場合 - デフォルト)
# DATABASE_URL は設定しない（空のままにする）

# フロントエンドURL (CORS設定用)
FRONTEND_URL=http://localhost:3000
